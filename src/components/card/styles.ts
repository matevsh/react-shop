import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
	cardHeader: {
		textDecoration: 'none',
		height: '100%',
		width: '100%',
		background: 'white',
		display: 'flex',
		justifyContent: 'center',
		padding: '8px',
	},
	link: {
		textDecoration: 'none',
	},
}));
